# DarkAgent Pro - Variáveis de Ambiente
# Copie este arquivo para .env e preencha com seus valores reais.
# .env está no .gitignore — nunca commitar!

# Chave da API do Google Gemini (https://aistudio.google.com/app/apikey)
# Tem precedência sobre o valor salvo em config/config.json.
GEMINI_API_KEY=
